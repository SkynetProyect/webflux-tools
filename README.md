# Webflux-Tools
all projects here are supose to be asyncronous webflux java projects.
Implementing the less reflective code as possible so it can be
compiled with graalvm.
if it doesnt compile with ./gradlew nativeCompile then check your coude.

Java version needed to be installed: sdk install java 21.0.2-graalce